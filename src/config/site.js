// Centralized brand/site constants for the EXPOVIA migration.
// SITE_URL is the canonical production origin — no trailing slash, no www.
export const SITE_NAME = "VIAFA";

export const SITE_URL = "https://expoviafair.com";

export const CONTACT_EMAIL = "info@expoviafair.com";

// WhatsApp contact line (same number as the second phone on the Contact page).
// International format, digits only — the shape wa.me expects.
export const WHATSAPP_NUMBER = "905427895593";
